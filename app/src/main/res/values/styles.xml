<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/contentBackground</item>
        <item name="background">@color/contentBackground</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/contentBackground</item>
        <item name="textInputStyle">@style/DefaultEditTextStyle</item>
        <item name="android:statusBarColor">@color/contentBackground</item>
    </style>

    <style name="SplashTheme" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/contentBackground</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="windowSplashScreenAnimatedIcon">@color/contentBackground</item>
        <item name="android:statusBarColor">@color/contentBackground</item>
    </style>

    <style name="RegistrationButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="DefaultEditTextStyle" parent="Widget.MaterialComponents.TextInputEditText.FilledBox">
        <item name="android:background">@color/transparent</item>
        <item name="android:padding">0dp</item>
    </style>

</resources>
