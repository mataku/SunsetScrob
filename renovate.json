{
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": ["^Deps.kt$"],
      "matchStrings": ["^ +const val [a-zA-Z]+ = \"(?<depName>.*?):(?<currentValue>.*?)\"\n$"],
      "datasourceTemplate": "maven",
      "depNameTemplate": "gradle"
    }
  ]
}
